# Local/example environment variables for MoveAndClean
# Copy this file to `.env` for local testing (never commit secrets).

# Supabase (frontend - anon key safe for browser)
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your_anon_key_here

# API base used by frontend to call your hosted backend (render/other)
# Example: https://moveandclean-backend.onrender.com
VITE_API_BASE=https://your-backend-hostname

# Supabase (backend - server only)
# WARNING: service_role key must never be exposed to the browser or committed.
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_KEY=your_service_role_key_here

# Other optional values
NODE_ENV=development
